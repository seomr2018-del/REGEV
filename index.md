---
title: דאשבורד קליטת עובדים – אפיון
lang: he
dir: rtl
---

<div dir="rtl" markdown="1">

> עמוד הבית של הפרויקט. התוכן הוא העתק של [`SPEC.MD`](https://github.com/seomr2018-del/REGEV/blob/claude/awesome-bell-ahmdl4/SPEC.MD), שנשאר מקור האמת.
> הנחיות העבודה ותיעוד הפרויקט: [Practice](./Practice.html).

# אפיון: דאשבורד משאבי אנוש – קליטת עובדים (Onboarding Dashboard)

> גרסה: 1.0 · תאריך: 22/09/2026 · סטטוס: טיוטה לאישור

---

## 1. מטרה ורקע

מחלקת משאבי אנוש מנהלת כיום את קליטת העובדים החדשים בגיליונות, מיילים ורשימות נפרדות. המטרה היא דאשבורד אחד שמרכז את **כל העובדים שנמצאים בתהליך קליטה**, מציג בכל רגע באיזה שלב כל אחד נמצא, ומאפשר לזהות מהר עיכובים.

### יעדים
- תמונת מצב של כל הקליטות הפעילות במסך אחד.
- מעקב אחר ארבעת שלבי החובה: טפסים, חתימה, הדרכת בטיחות, ציוד מחשוב.
- טבלת משימות לכל עובד, עם אחראי ותאריך יעד.
- התראה על משימות שעברו את תאריך היעד.

### מחוץ לתחום (גרסה 1)
- חתימה דיגיטלית בפועל (הדאשבורד רק משקף את הסטטוס).
- אינטגרציה עם מערכת שכר.
- פורטל לעובד החדש עצמו.

---

## 2. משתמשים והרשאות

| תפקיד | מה רואה | מה יכול לעשות |
|---|---|---|
| **מנהל/ת משאבי אנוש** | כל העובדים בקליטה | יצירה/עריכה/מחיקה של עובד ומשימות, שינוי כל סטטוס |
| **רכז/ת גיוס** | כל העובדים בקליטה | עדכון סטטוס טפסים וחתימה, הוספת הערות |
| **ממונה בטיחות** | כל העובדים בקליטה | עדכון סטטוס הדרכת בטיחות בלבד |
| **צוות IT** | כל העובדים בקליטה | עדכון סטטוס ציוד מחשוב בלבד |
| **מנהל/ת ישיר/ה** | רק העובדים בצוות שלו/ה | צפייה והוספת הערות |

---

## 3. מודל הנתונים

### 3.1 עובד חדש (`Employee`)

| שדה | סוג | חובה | הערות |
|---|---|---|---|
| `id` | string (UUID) | ✔ | |
| `fullName` | string | ✔ | |
| `email` | string | ✔ | |
| `phone` | string | | |
| `role` | string | ✔ | תפקיד |
| `department` | string | ✔ | מחלקה |
| `managerId` | string | ✔ | מנהל/ת ישיר/ה |
| `startDate` | date | ✔ | תאריך תחילת עבודה |
| `avatarUrl` | string | | |
| `steps` | `OnboardingSteps` | ✔ | ראו 3.2 |
| `tasks` | `Task[]` | ✔ | ראו 3.3 |
| `createdAt` / `updatedAt` | datetime | ✔ | |

### 3.2 שלבי קליטה (`OnboardingSteps`)

ארבעה שלבי חובה, לכל אחד סטטוס משלו:

| מפתח | תווית בממשק | אחראי ברירת מחדל |
|---|---|---|
| `forms` | טפסים הושלמו | רכז/ת גיוס |
| `signature` | ממתין לחתימה | רכז/ת גיוס |
| `safetyTraining` | הדרכת בטיחות | ממונה בטיחות |
| `equipment` | ציוד מחשוב סופק | צוות IT |

ערכי סטטוס אפשריים לכל שלב (`StepStatus`):

| ערך | תווית | צבע (Tailwind) |
|---|---|---|
| `not_started` | טרם התחיל | `slate` |
| `in_progress` | בתהליך | `amber` |
| `pending` | ממתין (לחתימה / לאישור) | `sky` |
| `done` | הושלם | `emerald` |
| `blocked` | חסום | `rose` |

**התקדמות כוללת** של עובד = אחוז השלבים והמשימות בסטטוס `done`.

**סטטוס כולל של עובד** (מחושב):
- `completed` – כל ארבעת השלבים וכל משימות החובה הושלמו.
- `at_risk` – יש משימה שעבר תאריך היעד שלה, או שלב חסום, או שנותרו פחות מ־3 ימים לתחילת העבודה והשלבים לא הושלמו.
- `on_track` – כל השאר.

### 3.3 משימה (`Task`)

| שדה | סוג | חובה | הערות |
|---|---|---|---|
| `id` | string | ✔ | |
| `employeeId` | string | ✔ | |
| `title` | string | ✔ | למשל "פתיחת משתמש במייל" |
| `category` | enum | ✔ | `forms` / `signature` / `safetyTraining` / `equipment` / `other` |
| `assignee` | string | ✔ | שם או מזהה האחראי |
| `dueDate` | date | ✔ | |
| `status` | `StepStatus` | ✔ | |
| `required` | boolean | ✔ | משימת חובה או רשות |
| `notes` | string | | |
| `completedAt` | datetime | | מתמלא אוטומטית במעבר ל־`done` |

### 3.4 תבנית משימות ברירת מחדל

כשנוצר עובד חדש, נוצרות לו אוטומטית המשימות הבאות (תאריך היעד יחסי לתאריך תחילת העבודה):

| משימה | קטגוריה | יעד |
|---|---|---|
| מילוי טופס 101 | forms | 7 ימים לפני |
| פרטי חשבון בנק | forms | 7 ימים לפני |
| צילום תעודת זהות | forms | 7 ימים לפני |
| חתימה על חוזה העסקה | signature | 5 ימים לפני |
| חתימה על הסכם סודיות (NDA) | signature | 5 ימים לפני |
| הדרכת בטיחות כללית | safetyTraining | 3 ימים אחרי |
| חתימה על טופס הדרכת בטיחות | safetyTraining | 3 ימים אחרי |
| הזמנת מחשב נייד | equipment | 10 ימים לפני |
| פתיחת משתמשים (מייל, מערכות) | equipment | 2 ימים לפני |
| מסירת ציוד וחתימה על קבלה | equipment | ביום הראשון |

---

## 4. מסכים ופונקציונליות

### 4.1 מבנה כללי (Layout)
- **סרגל צד (Sidebar)** – ניווט: סקירה כללית · עובדים · משימות · הגדרות. ניתן לכיווץ; במובייל נפתח כמגירה.
- **כותרת עליונה (Header)** – שדה חיפוש גלובלי, כפתור "הוספת עובד", החלפת מצב בהיר/כהה, תפריט משתמש.
- **כיוון RTL** – כל הממשק בעברית ומימין לשמאל (`dir="rtl"`).

### 4.2 מסך סקירה כללית (Overview) – `/`

**א. שורת מדדים (KPI Cards)** – ארבעה כרטיסים בראש המסך:

| כרטיס | ערך |
|---|---|
| עובדים בקליטה | מספר העובדים שלא בסטטוס `completed` |
| מתחילים השבוע | עובדים ש־`startDate` שלהם ב־7 הימים הקרובים |
| משימות באיחור | משימות שעבר תאריך היעד ולא הושלמו |
| קליטות שהושלמו החודש | עובדים שהגיעו ל־`completed` בחודש הנוכחי |

**ב. פילוח לפי שלב (Status Breakdown)** – לכל אחד מארבעת השלבים: פס התקדמות אופקי שמראה כמה עובדים סיימו/בתהליך/טרם התחילו. לחיצה על שלב מסננת את רשימת העובדים.

- טפסים הושלמו — `X / N`
- ממתין לחתימה — `X / N`
- הדרכת בטיחות — `X / N`
- ציוד מחשוב סופק — `X / N`

**ג. רשימת עובדים בקליטה (Employee Grid)** – כרטיס לכל עובד:
- תמונה/ראשי תיבות, שם, תפקיד, מחלקה.
- תאריך תחילה + ספירה לאחור ("מתחיל בעוד 4 ימים").
- ארבעה אייקוני שלבים צבעוניים (לפי הסטטוס) עם Tooltip.
- טבעת/פס התקדמות כוללת באחוזים.
- תג סטטוס כולל: "במסלול" / "בסיכון" / "הושלם".
- לחיצה על הכרטיס פותחת את מסך העובד.

**סינון ומיון:** לפי מחלקה, מנהל, סטטוס כולל, שלב שלא הושלם, טווח תאריכי תחילה. מיון לפי תאריך תחילה (ברירת מחדל), התקדמות, שם.

**ד. פיד פעילות אחרונה (Activity Feed)** – 10 העדכונים האחרונים ("דנה כהן סימנה 'ציוד סופק' עבור יוסי לוי · לפני שעה").

### 4.3 מסך עובד – `/employees/:id`

- **כותרת:** פרטי העובד, התקדמות כוללת, תג סטטוס, כפתור עריכה.
- **ציר שלבים (Stepper):** ארבעת השלבים בסדר כרונולוגי, כל אחד עם סטטוס, אחראי ותאריך עדכון אחרון. ניתן לשנות סטטוס ישירות מתוך השלב (לפי הרשאה).
- **טבלת מעקב משימות** – ראו 4.4.
- **הערות:** שרשור הערות חופשיות עם שם כותב וזמן.

### 4.4 טבלת מעקב משימות (Task Table)

מופיעה במסך העובד (משימות של עובד אחד) ובמסך `/tasks` (כל המשימות של כל העובדים, עם עמודת "עובד" נוספת).

| עמודה | תוכן | אינטראקציה |
|---|---|---|
| ☐ | תיבת סימון | בחירה מרובה לפעולות קבוצתיות |
| משימה | כותרת + אייקון קטגוריה | לחיצה פותחת חלונית עריכה |
| עובד | שם + תמונה | רק במסך `/tasks` |
| קטגוריה | תג צבעוני | סינון |
| אחראי | שם | סינון, עריכה מהירה |
| תאריך יעד | תאריך; באדום אם עבר | מיון |
| סטטוס | Dropdown צבעוני | שינוי ישיר בשורה |
| חובה | ✔ / — | |
| פעולות | ⋯ | עריכה, מחיקה, שכפול |

**יכולות הטבלה:**
- מיון לפי כל עמודה.
- סינון לפי קטגוריה, סטטוס, אחראי, "באיחור בלבד".
- חיפוש טקסט חופשי.
- פעולות קבוצתיות: שינוי סטטוס, שינוי אחראי, מחיקה.
- הוספת משימה ידנית (כפתור "+ משימה").
- עימוד (Pagination) – 20 שורות בעמוד.
- ייצוא ל־CSV.
- במובייל: הטבלה הופכת לרשימת כרטיסים.

### 4.5 הוספה/עריכת עובד – Modal
טופס עם השדות מסעיף 3.1, ולידציה בצד הלקוח, ואפשרות לבחור אם ליצור את תבנית המשימות מסעיף 3.4.

---

## 5. עיצוב וממשק (UI/UX)

### עקרונות
- נקי, מרווח, מודרני – הרבה white space, פינות מעוגלות (`rounded-xl`/`rounded-2xl`), צללים עדינים (`shadow-sm`).
- היררכיה ברורה: מדדים → פילוח → פירוט.
- צבע משמש **רק** להעברת סטטוס, לא לקישוט.
- תמיכה במצב כהה (`dark:` של Tailwind).
- רספונסיבי: דסקטופ (≥1280px) · טאבלט (768–1279px) · מובייל (<768px).

### מערכת עיצוב
| אלמנט | ערך |
|---|---|
| גופן | `Rubik` או `Heebo` (Google Fonts, תמיכה מלאה בעברית) |
| צבע ראשי | `indigo-600` |
| רקע | `slate-50` (בהיר) / `slate-900` (כהה) |
| משטחים | `white` / `slate-800` |
| טקסט | `slate-900` / `slate-100`; משני `slate-500` |
| צבעי סטטוס | ראו טבלה ב־3.2 |
| ריווח בסיס | יחידות של 4px (סקאלת Tailwind) |
| אייקונים | `lucide-react` |

### נגישות
- עמידה ב־WCAG 2.1 AA (ובתקן הישראלי 5568).
- סטטוס לעולם לא מועבר בצבע בלבד – תמיד גם טקסט או אייקון.
- ניווט מלא במקלדת, `focus-visible` ברור, תוויות `aria` לרכיבים אינטראקטיביים.
- ניגודיות מינימלית 4.5:1 לטקסט.

---

## 6. ארכיטקטורה טכנית

### 6.1 סטאק

| שכבה | טכנולוגיה |
|---|---|
| Build | **Vite** |
| שפה | **TypeScript** |
| UI | **React 18** |
| עיצוב | **Tailwind CSS** (+ `tailwindcss-rtl` / לוגיקת `ms-`/`me-` מובנית) |
| רכיבי בסיס | **shadcn/ui** (מבוסס Radix UI) |
| ניתוב | **React Router** |
| ניהול נתוני שרת | **TanStack Query** |
| טבלאות | **TanStack Table** |
| טפסים וולידציה | **React Hook Form** + **Zod** |
| תאריכים | **date-fns** (locale `he`) |
| גרפים | **Recharts** |
| אייקונים | **lucide-react** |
| Mock API (שלב פיתוח) | **MSW** (Mock Service Worker) |
| בדיקות | **Vitest** + **React Testing Library**; **Playwright** ל־E2E |
| איכות קוד | ESLint + Prettier |

### 6.2 מבנה תיקיות

```
src/
├── app/                 # App.tsx, router, providers
├── pages/
│   ├── OverviewPage.tsx
│   ├── EmployeePage.tsx
│   ├── TasksPage.tsx
│   └── SettingsPage.tsx
├── components/
│   ├── layout/          # Sidebar, Header, AppShell
│   ├── overview/        # KpiCard, StatusBreakdown, EmployeeCard, ActivityFeed
│   ├── employee/        # EmployeeHeader, OnboardingStepper, NotesThread
│   ├── tasks/           # TaskTable, TaskRow, TaskFilters, TaskDrawer
│   ├── forms/           # EmployeeForm, TaskForm
│   └── ui/              # רכיבי בסיס (shadcn): Button, Badge, Card, Dialog...
├── hooks/               # useEmployees, useTasks, useOnboardingStats
├── api/                 # client.ts, employees.ts, tasks.ts
├── mocks/               # MSW handlers + נתוני דמה
├── lib/                 # status.ts (חישובי סטטוס), dates.ts, utils.ts
└── types/               # Employee, Task, StepStatus
```

### 6.3 API (REST)

| מתודה | נתיב | תיאור |
|---|---|---|
| GET | `/api/employees` | רשימת עובדים בקליטה (תומך בפרמטרי סינון) |
| GET | `/api/employees/:id` | עובד בודד כולל שלבים ומשימות |
| POST | `/api/employees` | יצירת עובד (+ תבנית משימות) |
| PATCH | `/api/employees/:id` | עדכון פרטים |
| PATCH | `/api/employees/:id/steps/:step` | עדכון סטטוס שלב |
| DELETE | `/api/employees/:id` | מחיקה/ארכוב |
| GET | `/api/tasks` | כל המשימות (סינון, מיון, עימוד) |
| POST | `/api/employees/:id/tasks` | הוספת משימה |
| PATCH | `/api/tasks/:id` | עדכון משימה |
| PATCH | `/api/tasks/bulk` | עדכון קבוצתי |
| DELETE | `/api/tasks/:id` | מחיקת משימה |
| GET | `/api/stats/overview` | נתוני KPI ופילוח שלבים |
| GET | `/api/activity?limit=10` | פיד פעילות |

בשלב הראשון ה־API ממומש ב־MSW עם נתוני דמה, כך שהפרונט ניתן לפיתוח ולהדגמה בלי שרת.

---

## 7. רשימת רכיבים טכניים שייבנו

### 7.1 מבנה ותשתית
| # | רכיב | תיאור |
|---|---|---|
| 1 | `AppShell` | מעטפת: Sidebar + Header + אזור תוכן, RTL |
| 2 | `Sidebar` | ניווט ראשי, מצב מכווץ, מגירה במובייל |
| 3 | `Header` | חיפוש, כפתור הוספה, מצב כהה, תפריט משתמש |
| 4 | `ThemeProvider` | ניהול מצב בהיר/כהה |
| 5 | `QueryProvider` | הגדרת TanStack Query |
| 6 | `ProtectedRoute` | הגבלת מסכים ופעולות לפי תפקיד |

### 7.2 סקירה כללית
| # | רכיב | תיאור |
|---|---|---|
| 7 | `KpiCard` | כרטיס מדד: כותרת, ערך, אייקון, שינוי לעומת תקופה קודמת |
| 8 | `KpiRow` | שורת 4 כרטיסי מדדים |
| 9 | `StatusBreakdown` | פילוח ארבעת השלבים עם פסי התקדמות מוערמים |
| 10 | `EmployeeCard` | כרטיס עובד עם שלבים, התקדמות ותג סטטוס |
| 11 | `EmployeeGrid` | רשת כרטיסים + סינון ומיון |
| 12 | `EmployeeFilters` | מסנני מחלקה, מנהל, סטטוס, שלב, תאריכים |
| 13 | `ActivityFeed` | רשימת עדכונים אחרונים |

### 7.3 מסך עובד
| # | רכיב | תיאור |
|---|---|---|
| 14 | `EmployeeHeader` | פרטי עובד, התקדמות, פעולות |
| 15 | `OnboardingStepper` | ציר ארבעת השלבים עם שינוי סטטוס |
| 16 | `StepItem` | שלב בודד בציר |
| 17 | `NotesThread` | שרשור הערות + טופס הוספה |

### 7.4 משימות
| # | רכיב | תיאור |
|---|---|---|
| 18 | `TaskTable` | טבלת TanStack Table: מיון, סינון, בחירה, עימוד |
| 19 | `TaskFilters` | סרגל מסננים וחיפוש |
| 20 | `TaskStatusSelect` | Dropdown שינוי סטטוס בשורה |
| 21 | `BulkActionsBar` | סרגל פעולות קבוצתיות כשנבחרו שורות |
| 22 | `TaskDrawer` | חלונית צד לעריכת משימה |
| 23 | `TaskCardList` | תצוגת כרטיסים חלופית במובייל |
| 24 | `ExportCsvButton` | ייצוא הטבלה המסוננת ל־CSV |

### 7.5 טפסים
| # | רכיב | תיאור |
|---|---|---|
| 25 | `EmployeeFormDialog` | הוספה/עריכת עובד (RHF + Zod) |
| 26 | `TaskForm` | הוספה/עריכת משימה |
| 27 | `ConfirmDialog` | אישור מחיקה ופעולות הרסניות |

### 7.6 רכיבי UI משותפים
| # | רכיב | תיאור |
|---|---|---|
| 28 | `StatusBadge` | תג סטטוס: צבע + אייקון + טקסט |
| 29 | `ProgressBar` / `ProgressRing` | התקדמות באחוזים |
| 30 | `Avatar` | תמונה או ראשי תיבות |
| 31 | `EmptyState` | מצב ריק עם הסבר וקריאה לפעולה |
| 32 | `Skeleton` | שלדי טעינה לכרטיסים ולטבלה |
| 33 | `ErrorState` | הודעת שגיאה + ניסיון חוזר |
| 34 | `Toast` | הודעות הצלחה/שגיאה אחרי פעולה |
| 35 | `Tooltip` | הסבר קצר במעבר עכבר/פוקוס |

### 7.7 לוגיקה ו־Hooks
| # | רכיב | תיאור |
|---|---|---|
| 36 | `useEmployees` / `useEmployee` | שליפת עובדים ועובד בודד |
| 37 | `useTasks` / `useUpdateTask` | שליפה ועדכון משימות (כולל Optimistic Update) |
| 38 | `useOnboardingStats` | נתוני KPI ופילוח |
| 39 | `lib/status.ts` | חישוב התקדמות וסטטוס כולל (`on_track` / `at_risk` / `completed`) |
| 40 | `lib/dates.ts` | חישובי תאריכים יחסיים, איחורים, פורמט עברי |
| 41 | `mocks/handlers.ts` | Mock API ונתוני דמה (~15 עובדים) |

---

## 8. דרישות לא־פונקציונליות

- **ביצועים:** טעינה ראשונית < 2 שניות; תגובה לשינוי סטטוס < 200ms (Optimistic UI).
- **היקף:** תמיכה בעד 500 עובדים פעילים ו־10,000 משימות ללא ירידה בביצועים.
- **אבטחה:** הזדהות (SSO/OAuth בשלב השרת); הרשאות נאכפות גם בשרת; מידע אישי (ת"ז, בנק) לא מוצג בדאשבורד.
- **תאימות דפדפנים:** שתי הגרסאות האחרונות של Chrome, Edge, Safari, Firefox.
- **בדיקות:** כיסוי ≥ 80% ל־`lib/`; בדיקות רכיבים לטבלה ולטפסים; תרחיש E2E: הוספת עובד → השלמת כל השלבים → סטטוס "הושלם".

---

## 9. שלבי פיתוח

| שלב | תכולה |
|---|---|
| **1 – תשתית** | Vite + TS + Tailwind + shadcn/ui, AppShell, RTL, מצב כהה, MSW עם נתוני דמה |
| **2 – סקירה כללית** | KPI, פילוח שלבים, רשת כרטיסי עובדים, סינון |
| **3 – מסך עובד וטבלת משימות** | Stepper, TaskTable מלאה, עריכה בשורה, פעולות קבוצתיות |
| **4 – טפסים** | הוספה/עריכת עובד ומשימה, תבנית משימות |
| **5 – ליטוש** | נגישות, מובייל, Skeletons, ייצוא CSV, פיד פעילות, בדיקות |
| **6 – חיבור לשרת** | החלפת MSW ב־API אמיתי, הזדהות והרשאות |

---

## 10. קריטריוני קבלה

- [ ] מסך הסקירה מציג את ארבעת מדדי ה־KPI עם ערכים נכונים לפי נתוני הדמה.
- [ ] לכל עובד מוצגים ארבעת השלבים (טפסים, חתימה, בטיחות, ציוד) עם סטטוס עדכני.
- [ ] שינוי סטטוס של משימה או שלב מתעדכן מיידית בכרטיס, בפילוח ובמדדים.
- [ ] משימה שעבר תאריך היעד שלה מסומנת באדום ונספרת ב"משימות באיחור".
- [ ] הטבלה תומכת במיון, סינון, חיפוש, בחירה מרובה ועימוד.
- [ ] הממשק מוצג כהלכה ב־RTL, במצב בהיר וכהה, ובמובייל.
- [ ] כל הפעולות זמינות במקלדת ועוברות בדיקת axe ללא שגיאות קריטיות.

</div>
