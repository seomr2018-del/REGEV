# Practice – הנחיות ותהליך עבודה: דאשבורד קליטת עובדים

> מסמך זה מתעד איך עובדים על הפרויקט: ההנחיות, תהליך העבודה, השלבים שבוצעו עד כה ופקודות ההפעלה.
> האפיון המלא של המוצר נמצא ב־[`SPEC.MD`](./SPEC.MD).

---

## 1. מצב נוכחי – בקצרה

| נושא | מצב |
|---|---|
| אפיון (`SPEC.MD`) | ✅ נכתב ונדחף |
| מסמך תהליך (`Practice.md`) | ✅ המסמך הזה |
| קוד אפליקציה | ⏳ טרם התחיל – השלב הבא הוא הקמת התשתית |
| ענף עבודה | `claude/awesome-bell-ahmdl4` |
| ריפו | `seomr2018-del/REGEV` |

> **חשוב:** פקודות ההפעלה בסעיף 5 הן הפקודות **המתוכננות** לפי הסטאק שנבחר באפיון. הן יתחילו לעבוד אחרי שלב 1 (הקמת תשתית), כשיתווסף `package.json`.

---

## 2. הנחיות הפרויקט

### 2.1 כללי
- **האפיון הוא מקור האמת.** כל פיצ'ר נבנה לפי `SPEC.MD`. שינוי בדרישות → מעדכנים קודם את `SPEC.MD`, ורק אחר כך את הקוד.
- **שפת הממשק:** עברית, כיוון RTL (`dir="rtl"`) בכל המסכים.
- **שפת הקוד:** אנגלית – שמות משתנים, רכיבים, קבצים והודעות קומיט. טקסטים שמוצגים למשתמש – בעברית.
- **גרסה ראשונה עובדת עם נתוני דמה** (MSW), בלי שרת אמיתי.

### 2.2 סטאק (לפי SPEC.MD סעיף 6)
Vite · TypeScript · React 18 · Tailwind CSS · shadcn/ui · React Router · TanStack Query · TanStack Table · React Hook Form + Zod · date-fns · Recharts · lucide-react · MSW · Vitest + React Testing Library · Playwright · ESLint + Prettier.

### 2.3 קונבנציות קוד
- רכיבים ב־`PascalCase` (`EmployeeCard.tsx`), hooks ב־`camelCase` עם קידומת `use` (`useTasks.ts`).
- רכיב אחד לקובץ; מבנה התיקיות לפי `SPEC.MD` סעיף 6.2.
- TypeScript במצב `strict`; בלי `any`.
- עיצוב רק באמצעות מחלקות Tailwind; בלי קבצי CSS נפרדים מלבד `index.css` הגלובלי.
- ב־RTL משתמשים במאפיינים לוגיים: `ms-*`/`me-*`, `ps-*`/`pe-*`, `start-*`/`end-*` – לא `ml-*`/`mr-*`/`left-*`/`right-*`.
- צבעי סטטוס מוגדרים במקום אחד (`lib/status.ts`) ולא מפוזרים ברכיבים.
- סטטוס לעולם לא מועבר בצבע בלבד – תמיד עם טקסט או אייקון (נגישות).
- לוגיקה עסקית (חישוב התקדמות, איחורים, סטטוס כולל) נמצאת ב־`lib/` כפונקציות טהורות, עם בדיקות.

### 2.4 Git
- עובדים על ענף הפיצ'ר, לא ישירות על `main`.
- הודעות קומיט באנגלית, בזמן הווה ובציווי: `Add TaskTable sorting`, `Fix RTL alignment in Sidebar`.
- קומיט אחד = שינוי לוגי אחד.
- לפני דחיפה: lint, typecheck ובדיקות עוברים (ראו סעיף 5.3).
- דחיפה: `git push -u origin <branch>`.
- Pull Request נפתח רק כשמחליטים על כך במפורש.

---

## 3. תהליך העבודה

כל שלב פיתוח עובר את אותו מחזור:

1. **הגדרה** – בוחרים שלב/פיצ'ר מתוך `SPEC.MD` (סעיף 9) ומוודאים שהדרישות ברורות. אם לא – מעדכנים את האפיון.
2. **פיתוח** – כותבים את הרכיבים והלוגיקה לפי הקונבנציות בסעיף 2.3.
3. **בדיקות** – בדיקות יחידה ללוגיקה, בדיקות רכיבים לטבלאות ולטפסים.
4. **בדיקה ויזואלית** – מריצים את האפליקציה ובודקים: RTL, מצב כהה, מובייל, ניווט במקלדת.
5. **אימות** – `lint` + `typecheck` + `test` עוברים נקי.
6. **קומיט ודחיפה** – לענף העבודה.
7. **עדכון המסמך הזה** – מסמנים את השלב כבוצע ביומן בסעיף 4.

---

## 4. יומן שלבים – מה עשינו עד עכשיו

### ✅ שלב 0.1 – כתיבת אפיון (`SPEC.MD`)
**בקשה:** קובץ אפיון מפורט לדאשבורד משאבי אנוש לקליטת עובדים, שכולל סקירת עובדים בקליטה, טבלת משימות, ממשק מודרני ב־React + Tailwind ורשימת רכיבים.

**מה נקבע באפיון:**
- **ארבעה שלבי חובה** לכל עובד: טפסים הושלמו · ממתין לחתימה · הדרכת בטיחות · ציוד מחשוב סופק.
- **חמישה סטטוסים** לשלב/משימה: טרם התחיל · בתהליך · ממתין · הושלם · חסום.
- **סטטוס כולל מחושב** לעובד: במסלול / בסיכון / הושלם.
- **תבנית של 10 משימות** שנוצרות אוטומטית לכל עובד חדש.
- **מסכים:** סקירה כללית (KPI, פילוח שלבים, כרטיסי עובדים, פיד פעילות), מסך עובד, מסך משימות, טופסי הוספה/עריכה.
- **הרשאות** לפי תפקיד: משאבי אנוש, גיוס, ממונה בטיחות, IT, מנהל ישיר.
- **41 רכיבים** בשבע קבוצות, רשימת קריאות API, מבנה תיקיות, שישה שלבי פיתוח וקריטריוני קבלה.

**החלטות פתוחות לאישור:**
- כלל "בסיכון": משימה באיחור, שלב חסום, או פחות מ־3 ימים לתחילת עבודה עם שלבים פתוחים.
- מחוץ לגרסה 1: חתימה דיגיטלית בפועל, חיבור למערכת שכר, פורטל לעובד.

**קומיט:** `Add SPEC.MD for HR onboarding dashboard`

### ✅ שלב 0.2 – מסמך תהליך (`Practice.md`)
תיעוד ההנחיות, תהליך העבודה, יומן השלבים ופקודות ההפעלה (המסמך הזה).

### ✅ שלב 0.3 – עמוד בית ל־GitHub Pages (`index.md`)
- `index.md` הוא העתק של `SPEC.MD`, עטוף ב־`<div dir="rtl">` כדי שהעמוד יוצג מימין לשמאל, עם קישור ל־Practice.
- `_config.yml` מגדיר את כותרת האתר, שפה (`he`) ותבנית `jekyll-theme-primer`.
- **`SPEC.MD` נשאר מקור האמת.** אחרי כל שינוי בו מעדכנים גם את `index.md` (סעיף 5.7).

### ⏳ השלבים הבאים (לפי SPEC.MD סעיף 9)

| שלב | תכולה | מצב |
|---|---|---|
| 1 – תשתית | Vite + TS + Tailwind + shadcn/ui, AppShell, RTL, מצב כהה, MSW עם נתוני דמה | ⬜ |
| 2 – סקירה כללית | KPI, פילוח שלבים, רשת כרטיסי עובדים, סינון | ⬜ |
| 3 – מסך עובד וטבלת משימות | Stepper, TaskTable, עריכה בשורה, פעולות קבוצתיות | ⬜ |
| 4 – טפסים | הוספה/עריכת עובד ומשימה, תבנית משימות | ⬜ |
| 5 – ליטוש | נגישות, מובייל, Skeletons, ייצוא CSV, פיד פעילות, בדיקות | ⬜ |
| 6 – חיבור לשרת | API אמיתי, הזדהות והרשאות | ⬜ |

---

## 5. פקודות הפעלה

> הפקודות בסעיפים 5.2–5.4 זמינות אחרי שלב 1. דרישות מוקדמות: **Node.js 20+** ו־**npm 10+**.

### 5.1 קבלת הקוד
```bash
git clone https://github.com/seomr2018-del/REGEV.git
cd REGEV
git checkout claude/awesome-bell-ahmdl4
```

### 5.2 התקנה והרצה בפיתוח
```bash
npm install          # התקנת תלויות
npm run dev          # שרת פיתוח עם נתוני דמה (MSW) – http://localhost:5173
```

### 5.3 בדיקות ואיכות קוד
```bash
npm run lint         # ESLint
npm run format       # Prettier
npm run typecheck    # tsc --noEmit
npm test             # Vitest – בדיקות יחידה ורכיבים
npm run test:e2e     # Playwright – בדיקות קצה לקצה
```

לפני כל דחיפה:
```bash
npm run lint && npm run typecheck && npm test
```

### 5.4 בנייה לייצור
```bash
npm run build        # בנייה לתיקיית dist/
npm run preview      # הרצה מקומית של גרסת הייצור
```

### 5.5 הקמת התשתית (שלב 1 – להרצה פעם אחת)
הפקודות שיקימו את הפרויקט בשלב 1:
```bash
npm create vite@latest . -- --template react-ts
npm install
npm install -D tailwindcss @tailwindcss/vite
npx shadcn@latest init
npm install react-router-dom @tanstack/react-query @tanstack/react-table \
  react-hook-form zod @hookform/resolvers date-fns recharts lucide-react
npm install -D msw vitest @testing-library/react @testing-library/jest-dom jsdom \
  @playwright/test eslint prettier
npx msw init public/ --save
```

### 5.6 Git – עבודה יומיומית
```bash
git status                                   # מה השתנה
git add <files>                              # הוספת קבצים לקומיט
git commit -m "Add <feature>"                # קומיט
git push -u origin claude/awesome-bell-ahmdl4  # דחיפה לענף העבודה
git pull origin claude/awesome-bell-ahmdl4     # משיכת עדכונים
```

### 5.7 סנכרון עמוד הבית (`index.md`) אחרי שינוי ב־SPEC.MD
מחליפים את תוכן האפיון ב־`index.md` (כל מה שבין שורת ההפניה בראש הקובץ לבין `</div>` בסופו) בתוכן העדכני של `SPEC.MD`, ואז:
```bash
git add SPEC.MD index.md
git commit -m "Update spec and sync index.md"
git push -u origin claude/awesome-bell-ahmdl4
```
